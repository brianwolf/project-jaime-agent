FROM ghcr.io/jaime-project/jaime-agent:1.7.0

ENV AGENT_TYPE=KUBERNETES

COPY resources/kubectl.tar.gz . 
RUN tar -xf kubectl.tar.gz 
RUN rm kubectl.tar.gz

RUN chmod 755 kubectl
ENV PATH="$HOME:$PATH"
